<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="customerUserInfo"
            type="com.example.sipsupporterapp.model.CustomerUserResult.CustomerUserInfo" />

        <variable
            name="userViewModel"
            type="com.example.sipsupporterapp.viewmodel.UserViewModel" />

        <variable
            name="position"
            type="Integer" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/ripple"
        android:clickable="true"
        android:onClick="@{() -> userViewModel.onItemClick(position)}">

        <TextView
            android:id="@+id/tv_user_name"
            convertLetter="@{userViewModel.getCustomerUserInfoAt(position).userName}"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginRight="8dp"
            android:ellipsize="end"
            android:fontFamily="@font/one"
            android:maxLines="1"
            android:textColor="@color/white"
            android:textDirection="rtl"
            app:layout_constraintBottom_toBottomOf="@id/iv_status"
            app:layout_constraintEnd_toStartOf="@id/iv_status"
            app:layout_constraintTop_toTopOf="@id/iv_status" />

        <ImageView
            android:id="@+id/iv_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:layout_marginRight="8dp"
            android:layout_marginBottom="16dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@id/tv_row"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/tv_last_seen"
            android:layout_width="80dp"
            android:layout_height="wrap_content"
            android:layout_marginLeft="16dp"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="16dp"
            android:fontFamily="@font/one"
            android:gravity="start"
            android:maxLines="2"
            android:text="@{userViewModel.getCustomerUserInfoAt(position).lastSeen}"
            android:textColor="@color/white"
            android:textDirection="rtl"
            android:textSize="12sp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/tv_row"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginRight="8dp"
            android:fontFamily="@font/two"
            android:textColor="@color/white"
            android:textDirection="rtl"
            android:textSize="16sp"
            app:layout_constraintBottom_toBottomOf="@id/iv_status"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@id/iv_status" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>